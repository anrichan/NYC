<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarEexample3">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- アプリ名 -->
      <div class="navbar-brand"><%= link_to "NewYorkYaaaaas", root_path %>
      </div>
      </div>
    
    <div class="collapse navbar-collapse" id="navbarEexample3">
      <ul class="nav navbar-nav">
      <% if user_signed_in? %> 
        <li><%= link_to 'emaile edit', edit_user_registration_path %></li>
        <li class="active"><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <li><%= link_to "my page", "/users/#{current_user.id}" %></li>
      <% else %>
        <li class="active"><%= link_to "Sign in", new_user_registration_path %><li>
        <li><%= link_to "Login", new_user_session_path %></li>
      <% end %>
      <% if admin_signed_in? %> 
        <li><%= link_to "admin Logout", destroy_admin_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to "admin Sign in", new_admin_registration_path %></li> 
        <li><%= link_to "admin Login", new_admin_session_path %></li>
      <% end %>

        </ul>
      
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="検索キーワード">
        </div>
        <button type="submit" class="btn btn-default">検索</button>

      </form>
      <% if user_signed_in? %> 
       <% if current_user.profile.present? %>

      <div class="navbar-text navbar-right"><div class="profile_photo_header"><%= image_tag current_user.profile.photo.url(:thumb) %><strong><%= current_user.profile.user_name %></strong></div>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
